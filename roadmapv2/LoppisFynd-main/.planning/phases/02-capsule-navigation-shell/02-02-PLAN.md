---
phase: 02-capsule-navigation-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/shared/widgets/capsule_nav_bar.dart
  - test/fl_065_nav_smoke_test.dart
autonomous: false
requirements:
  - NAV-01
  - NAV-03

must_haves:
  truths:
    - "User can switch between exactly five tabs (Home, Scan, Haul, History, Profile) without route/name regression"
    - "Capsule nav shows an explicit selected bubble state for every destination (including the primary Scan)"
  artifacts:
    - "lib/shared/widgets/capsule_nav_bar.dart: selected vs unselected bubble styling for all destinations"
    - "test/fl_065_nav_smoke_test.dart: 5-tab contract assertions + switching smoke"
  key_links:
    - "test/fl_065_nav_smoke_test.dart -> lib/core/navigation/app_nav_shell.dart: taps nav_* keys to switch tabs"
---

<objective>
Make capsule nav selection visually explicit (including the primary Scan) and add test-level contract assertions that the shell exposes exactly 5 destinations.

Purpose: Meet the NAV-03 visual contract while preventing NAV-01 regressions in tab count/order/keys.
Output: CapsuleNavBar selection bubble rules + a nav smoke test that asserts the 5-tab contract.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capsule-navigation-shell/02-RESEARCH.md

@lib/shared/widgets/capsule_nav_bar.dart
@test/fl_065_nav_smoke_test.dart
@docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make selected bubble state explicit for the primary Scan destination</name>
  <files>lib/shared/widgets/capsule_nav_bar.dart</files>
  <action>
Update the capsule nav item styling so selected vs unselected is unambiguous for all destinations.

- Keep the existing non-primary behavior: selected shows a distinct bubble (fill + stroke), unselected is subdued.
- Fix the primary (`isPrimary: true`) behavior so it no longer appears selected when inactive.
- Preserve the "primary" prominence, but add a clear selected-only affordance (e.g. a ring/stroke/inner bubble) that toggles with `selected`.
- Do not change destination keys, semantics, or sizing tokens (use `AppCapsuleNav`, `AppRadius`, `AppColors`, `AppOpacity`, `AppShadows`).

Guardrails:
- Do not introduce ad-hoc design constants in shared widgets; use existing tokens.
- Keep blur clipped (do not add new BackdropFilter usage outside the existing clipped container).
  </action>
  <verify>flutter test test/fl_065_nav_smoke_test.dart</verify>
  <done>
- Primary Scan item has a distinct selected state vs unselected state.
- Non-primary items still have distinct selected vs unselected bubble state.
- No keys/labels/semantics regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 5-tab contract assertions to nav smoke test</name>
  <files>test/fl_065_nav_smoke_test.dart</files>
  <action>
Strengthen the existing nav smoke test to assert the tab contract is exactly five destinations.

- Keep the existing behavior checks (tapping each key switches and expected titles appear).
- Add an explicit assertion that the capsule nav exposes exactly 5 destinations using the 5 contract keys (robust to internal tappable widget changes).
  - Under `Key('capsule_nav')`, assert each destination key exists exactly once:
    - `nav_dashboard`, `nav_scanner`, `nav_haul`, `nav_history`, `nav_profile`
  - Prefer `find.descendant(of: find.byKey(Key('capsule_nav')), matching: find.byKey(Key('nav_dashboard')))` (repeat for each key).
  - Optionally (recommended if `CapsuleNavBar` remains the widget), also assert `tester.widget<CapsuleNavBar>(find.byType(CapsuleNavBar)).destinations.length == 5` so the test fails if extra destinations are added even if keys remain.
- Keep existing destination key usage so the test continues to guard against key/name regression.
  </action>
  <verify>flutter test test/fl_065_nav_smoke_test.dart</verify>
  <done>
- Test fails if an extra tab is added/removed, or if destinations are no longer tappable.
- Test continues to pass on CI with the new shell behavior from 02-01.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify bubble state + state retention against reference</name>
  <files>
lib/core/navigation/app_nav_shell.dart
lib/shared/widgets/capsule_nav_bar.dart
  </files>
  <action>
- Run the app: `flutter run`.
- Verify there are exactly 5 tabs: Home, Scan, Haul, History, Profile.
- Tap each tab and confirm the capsule shows a clear selected bubble state for the active destination (including Scan).
- In any scrollable tab (e.g. History), scroll down, switch away, switch back: scroll position should be preserved.
- Confirm content is not obscured by the capsule nav on your device (bottom of lists/forms remains reachable).

Resume signal:
- Type "approved" or describe what looks wrong vs `docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf`.
  </action>
  <verify>
- Record the result (approved or issue report). If issues: include device + what tab + what mismatch.
  </verify>
  <done>
- NAV-03 visual check is complete (approved or issue report detailed enough for a follow-up plan).
  </done>
</task>

</tasks>

<verification>
- `flutter test test/fl_065_nav_smoke_test.dart`
- Human verification against `docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf` for NAV-03
</verification>

<success_criteria>
- Capsule nav selection is visually explicit for every destination, including the primary Scan.
- Tests assert the nav exposes exactly five destinations and tab switching still works.
</success_criteria>

<output>
After completion, create `.planning/phases/02-capsule-navigation-shell/02-02-SUMMARY.md`
</output>
