abstract final class AppOpacity {
  /// Standard glass tile fill opacity.
  static const double glassTile = 0.40;

  /// Larger board surfaces (e.g. main bento containers).
  static const double glassBoard = 0.34;

  /// Backplate layers behind a board.
  static const double glassBackplate = 0.22;

  /// Motif overlay layer opacity (e.g. repeating logo wallpaper).
  static const double motifOverlay = 0.10;

  /// Capsule nav bar background fill opacity.
  static const double capsuleNavFill = 0.68;
}
