import 'package:flutter/material.dart';

import '../../shared/widgets/logo_motif_overlay.dart';

class LoginMotifLayer extends StatelessWidget {
  const LoginMotifLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const LogoMotifOverlay();
  }
}
