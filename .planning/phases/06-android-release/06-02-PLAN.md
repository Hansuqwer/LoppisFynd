# Phase 6 Plan 06-02: Android Signing Docs + ProGuard Rules + Adaptive Icon

**Requirements:** REL-03 (signing + ProGuard), REL-04 (adaptive icon)
**Status:** In Progress

## Tasks

### REL-03a: Remove stale TODO comments from build.gradle.kts
Lines 29 and 80 have TODO comments left over from template scaffolding.
The signing logic is already implemented. Remove the comments.

### REL-03b: Document signing setup in .env.example
Add signing key documentation block to `.env.example`.

### REL-03c: Add ProGuard rules
`android/app/proguard-rules.pro` currently only has MediaPipe dontwarn entries.
Add rules for: Drift, Sentry, Supabase/OkHttp/Realtime, Workmanager, Flutter.

### REL-04a: Create mipmap-anydpi-v26 adaptive icon XML
Create:
- `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`
- `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml`

### REL-04b: Create round icon PNG stubs
Copy `ic_launcher.png` to `ic_launcher_round.png` in each mipmap density folder.

### REL-04c: Add roundIcon to AndroidManifest.xml

## Verification
- `fvm dart analyze` — only 9 known baseline warnings
- `fvm flutter build apk --flavor dev --debug` — must succeed
