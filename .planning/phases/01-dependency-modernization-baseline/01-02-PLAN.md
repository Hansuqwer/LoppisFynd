---
phase: 01-dependency-modernization-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .github/workflows/ci.yml
  - android/app/build.gradle.kts
  - android/build.gradle.kts
  - android/gradle/wrapper/gradle-wrapper.properties
  - pubspec.lock
  - test/goldens/login_screen.png
  - test/goldens/dashboard_screen.png
  - test/goldens/history_empty.png
  - test/goldens/bento_card.png
  - test/goldens/draft_editor.png
autonomous: false
requirements:
  - DEP-05
user_setup: []

must_haves:
  truths:
    - "Full CI test pipeline passes after dependency updates"
    - "Android release appbundles build for staging and prod"
    - "Goldens (if changed) are intentionally updated and reviewed"
    - "User can manually smoke-test core flows on Android device and minimally on iOS"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Source of truth for CI verification steps"
    - path: "test/goldens/login_screen.png"
      provides: "Golden baseline for auth"
    - path: "test/goldens/dashboard_screen.png"
      provides: "Golden baseline for dashboard"
    - path: "test/goldens/history_empty.png"
      provides: "Golden baseline for history"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "flutter build appbundle"
      via: "CI build steps"
      pattern: "flutter build appbundle"
    - from: "test/features_.*_golden_test\.dart"
      to: "test/goldens/*.png"
      via: "golden assertions"
      pattern: "matchesGoldenFile"
---

<objective>
Run full verification gates (CI-equivalent + Android release builds) and complete required manual smoke-testing (Android device + minimal iOS) to confirm no regressions from Phase 1 upgrades.

Purpose: Prove Phase 1 upgrade baseline is stable enough to build on.
Output: Green CI commands, successful Android appbundle builds, and confirmed core-flow smoke test results.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-dependency-modernization-baseline/01-CONTEXT.md
@.planning/phases/01-dependency-modernization-baseline/01-RESEARCH.md

@.github/workflows/ci.yml
@android/app/build.gradle.kts
@test/features_auth/login_screen_golden_test.dart
@test/features_dashboard/dashboard_screen_golden_test.dart
@test/features_history/history_empty_golden_test.dart
@test/features_drafts/draft_editor_golden_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run CI-equivalent gates and Android release builds</name>
  <files>
.github/workflows/ci.yml
android/app/build.gradle.kts
android/build.gradle.kts
android/gradle/wrapper/gradle-wrapper.properties
pubspec.lock
  </files>
  <action>
- Run the same verification gates as CI (format/analyze/custom_lint/test) and fix any remaining failures.
- Treat test flakiness as a Phase 1 blocker: if tests are intermittent, fix the underlying cause (no skipping/quarantining as the default).
- Build Android release appbundles for both flavors exactly like CI:
  - `flutter build appbundle --flavor staging --release --dart-define=APP_ENV=staging`
  - `flutter build appbundle --flavor prod --release --dart-define=APP_ENV=prod`
- If any Android tooling config changes are required (Gradle/AGP/Kotlin/minSdk/compileSdk), keep them within Phase 1 scope: fix forward to stable, no feature changes.
  </action>
  <verify>
dart format --output=none --set-exit-if-changed lib test
flutter analyze
flutter pub run custom_lint
flutter test
flutter build appbundle --flavor staging --release --dart-define=APP_ENV=staging
flutter build appbundle --flavor prod --release --dart-define=APP_ENV=prod
  </verify>
  <done>
- All CI-equivalent commands pass locally.
- Both Android appbundles build successfully.
- Tests are stable (no new flakes introduced; flakes fixed rather than skipped).
  </done>
</task>

<task type="auto">
  <name>Task 2: If goldens changed, update intentionally and re-run tests</name>
  <files>
test/goldens/login_screen.png
test/goldens/dashboard_screen.png
test/goldens/history_empty.png
test/goldens/bento_card.png
test/goldens/draft_editor.png
  </files>
  <action>
- If `flutter test` fails only due to golden diffs introduced by Flutter/Skia/dependency upgrades:
  - Generate updated goldens: `flutter test --update-goldens`.
  - Re-run `flutter test` to confirm goldens are now stable.
- Do not update goldens to hide real layout bugs; only accept changes that are intended outcomes of the dependency/toolchain upgrades.
  </action>
  <verify>
flutter test
  </verify>
  <done>
- Goldens are either unchanged (tests pass), or updated intentionally and tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Manual smoke test + iOS build + golden review</name>
  <files>
test/goldens/login_screen.png
test/goldens/dashboard_screen.png
test/goldens/history_empty.png
test/goldens/bento_card.png
test/goldens/draft_editor.png
ios/Podfile
ios/Runner.xcodeproj/project.pbxproj
  </files>
  <action>
Claude completed: dependency upgrades + CI-equivalent gates + Android appbundle builds (and optionally updated goldens if needed). This checkpoint verifies the human-required parts of Phase 1 decisions and success criteria.

How to verify:
1) Android device smoke test (preferred target: Android 14/15 flagship per Phase 1 decisions)
   - Run: `flutter run --flavor staging --dart-define=APP_ENV=staging`
   - Verify core flows (no new crashes/runtime errors):
     - Capture/scan: open scanner, camera preview works, take a photo / scan barcode.
     - Item lifecycle: create item, edit fields, save, close app, reopen, item persists.
     - History/browse: open history/dashboard screens.
     - Settings: open settings, dev mode toggle still works (if present), sync toggles visible and non-broken.
     - Background: toggle market sync interval if available; confirm no immediate exceptions in logs.

2) iOS minimal smoke test (Phase 1 requirement; do on macOS)
   - From repo root on mac:
     - `flutter pub get`
     - `cd ios && pod install && cd ..`
     - `flutter build ios --no-codesign` (or `flutter run` on simulator)
   - Verify app launches and key screens open without runtime errors.

3) Golden review (only if goldens were updated)
   - Visually review diffs under `test/goldens/` and confirm changes are intended.
  </action>
  <verify>User completes steps and replies "approved" (or reports issues).</verify>
  <done>User typed "approved" (or provided concrete issues to address).</done>
</task>

</tasks>

<verification>
- CI parity: commands in `.github/workflows/ci.yml` succeed locally (except iOS which is manual on macOS).
- Android release builds succeed for staging and prod.
- Manual smoke test confirms no regressions in capture + catalog flows.
</verification>

<success_criteria>
- DEP-05 satisfied: full test suite + CI-equivalent steps pass after dependency updates.
- Android and iOS minimal runtime validation completed with no new crashes.
- Goldens reviewed if updated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-modernization-baseline/01-02-SUMMARY.md`
</output>
