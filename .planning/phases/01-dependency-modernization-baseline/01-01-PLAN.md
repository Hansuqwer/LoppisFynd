---
phase: 01-dependency-modernization-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/core/app/providers.dart
  - lib/core/database/app_database.dart
  - lib/core/database/app_database.g.dart
  - lib/main.dart
  - lib/features/scanner/scanner_screen.dart
  - lib/features/scanner/barcode/mlkit_input_image.dart
  - ios/Runner.xcodeproj/project.pbxproj
  - ios/Podfile
  - test/fl_010_database_test.dart
  - test/features_history/fl_052_hauls_dao_test.dart
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04
user_setup: []

must_haves:
  truths:
    - "Project resolves dependencies on Flutter stable and compiles cleanly"
    - "Riverpod upgrade compiles (legacy providers still work)"
    - "Camera + background sync compile after camera/workmanager updates"
    - "flutter analyze and flutter pub run custom_lint complete with 0 issues"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Pinned core dependency versions for Phase 1 baseline"
    - path: "pubspec.lock"
      provides: "Resolved dependency set for reproducible builds"
    - path: "lib/core/app/providers.dart"
      provides: "Riverpod providers compile under Riverpod 3 (legacy imports where needed)"
    - path: "ios/Runner.xcodeproj/project.pbxproj"
      provides: "iOS deployment target >= 14.0 for workmanager"
    - path: "ios/Podfile"
      provides: "CocoaPods wiring for Flutter plugins (iOS)"
  key_links:
    - from: "lib/core/app/providers.dart"
      to: "package:flutter_riverpod/legacy.dart"
      via: "import"
      pattern: "flutter_riverpod/legacy\.dart"
    - from: "pubspec.yaml"
      to: "flutter_riverpod"
      via: "dependency pin"
      pattern: "flutter_riverpod: 3\.2\.1"
    - from: "ios/Podfile"
      to: "Flutter podhelper"
      via: "require flutter_tools podhelper.rb"
      pattern: "podhelper\.rb"
---

<objective>
Modernize the Flutter dependency/toolchain baseline by upgrading the core packages (Riverpod, camera, workmanager) with tight pins, and fix forward any breakages until analyze/lints/tests are green.

Purpose: Establish a stable, current baseline before Phase 2 feature work.
Output: Updated dependencies + platform baselines + green analysis/tests on CI-equivalent commands.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-dependency-modernization-baseline/01-CONTEXT.md
@.planning/phases/01-dependency-modernization-baseline/01-RESEARCH.md

@pubspec.yaml
@.github/workflows/ci.yml
@lib/core/app/providers.dart
@lib/features/scanner/scanner_screen.dart
@lib/services/sync/background/background_sync.dart
@ios/Runner.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump iOS deployment target and restore Podfile</name>
  <files>
ios/Runner.xcodeproj/project.pbxproj
ios/Podfile
  </files>
  <action>
- Update `ios/Runner.xcodeproj/project.pbxproj` to set `IPHONEOS_DEPLOYMENT_TARGET = 14.0;` in all configurations (workmanager requires iOS >= 14.0; Phase 1 allows up to iOS 15 if later forced).
- Create `ios/Podfile` from the Flutter stable template and set `platform :ios, '14.0'`.
- Keep the Podfile consistent with Flutter tooling (use `flutter_root`, `podhelper.rb`, `flutter_ios_podfile_setup`, and `flutter_install_all_ios_pods`).
- Do not introduce any new iOS capabilities or app features in this phase; only unblock plugin builds.
  </action>
  <verify>
rg -n "IPHONEOS_DEPLOYMENT_TARGET = 14\\.0;" ios/Runner.xcodeproj/project.pbxproj
test -f ios/Podfile
rg -n "platform :ios, '14\\.0'" ios/Podfile
  </verify>
  <done>
- iOS deployment target is 14.0 in Xcode project.
- `ios/Podfile` exists and targets iOS 14.0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Riverpod + camera with tight pins and fix forward compile issues</name>
  <files>
pubspec.yaml
pubspec.lock
lib/core/app/providers.dart
lib/main.dart
lib/features/scanner/scanner_screen.dart
lib/features/scanner/barcode/mlkit_input_image.dart
  </files>
  <action>
- Update core dependency pins in `pubspec.yaml` (exact versions, not caret):
  - `flutter_riverpod: 3.2.1`
  - `camera: 0.11.4`
  - Keep Drift on its current stable, but prefer exact pins for the baseline: `drift: 2.31.0`, `drift_dev: 2.31.0`.
  - Prefer exact pinning for `workmanager: 0.9.0+3`.
- Run `flutter pub get` and adapt code to new APIs (fix forward).
- Apply Riverpod 3 migration rules:
  - Any file using `StateProvider`, `StateNotifierProvider`, or other legacy provider types must import `package:flutter_riverpod/legacy.dart`.
  - Keep non-legacy Riverpod imports on `package:flutter_riverpod/flutter_riverpod.dart`.
- While fixing forward, keep an eye on background tasks and camera usage:
  - Ensure `lib/services/sync/background/background_sync.dart` still compiles with the pinned `workmanager` version (callback dispatcher, initialization, and plugin registrant).
  - Ensure `lib/features/scanner/scanner_screen.dart` still compiles with the pinned `camera` version (controller init/lifecycle and `InputImage` wiring via `mlkit_input_image.dart`).
- Run `dart fix --apply` if it offers mechanical fixes, but do not accept any behavior changes that look like new features.
- Keep regression bar: `flutter analyze` and `flutter pub run custom_lint` must be clean (0 issues).
  </action>
  <verify>
flutter pub get
dart format --output=none --set-exit-if-changed lib test
flutter analyze --fatal-warnings --fatal-infos
flutter pub run custom_lint
flutter test
  </verify>
  <done>
- Core deps are upgraded and pinned (Riverpod 3.2.1, camera 0.11.4).
- App code compiles and `flutter test` passes on the local machine.
- No new analyzer/lint warnings are introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate Drift codegen + migrations/queries are still working</name>
  <files>
pubspec.yaml
pubspec.lock
lib/core/database/app_database.dart
lib/core/database/app_database.g.dart
test/fl_010_database_test.dart
test/features_history/fl_052_hauls_dao_test.dart
  </files>
  <action>
- Ensure the Drift generator/codegen pipeline still works with the upgraded toolchain and pinned versions.
- Run build_runner generation and include any resulting `.g.dart` changes in the worktree (this phase prefers tight pins + reproducible lockfile).
- Validate Drift runtime behavior via targeted tests (in addition to full `flutter test`):
  - Database open/connection behavior and basic queries.
  - DAO/query tests that cover common read/write paths.
- Data-safety constraint check (no destructive migrations):
  - If `schemaVersion` or `migration` logic is changed during fixes, keep migrations additive/transform-only (no drops/clears).
  </action>
  <verify>
dart run build_runner build --delete-conflicting-outputs
flutter test test/fl_010_database_test.dart test/features_history/fl_052_hauls_dao_test.dart
  </verify>
  <done>
- build_runner completes successfully and generated Drift files are consistent.
- Targeted database/DAO tests pass, indicating migrations/queries still work.
- No destructive migration steps were introduced.
  </done>
</task>

</tasks>

<verification>
- CI-equivalent gate on this machine: `dart format --output=none --set-exit-if-changed lib test && flutter analyze && flutter pub run custom_lint && flutter test`
- CI-equivalent gate on this machine (fatal warnings): `dart format --output=none --set-exit-if-changed lib test && flutter analyze --fatal-warnings --fatal-infos && flutter pub run custom_lint && flutter test`
- Riverpod 3 migration sanity: no missing legacy provider symbols; `lib/core/app/providers.dart` imports legacy as needed.
- iOS baseline prep: deployment target is >= 14.0 and Podfile exists (actual iOS build happens in Plan 01-02 checkpoint).
- Drift validation: `dart run build_runner build --delete-conflicting-outputs` succeeds and database/DAO tests pass.
</verification>

<success_criteria>
- Dependency baseline updated for Riverpod/camera/workmanager with tight pins.
- No regressions detectable via analyze/custom_lint/tests.
- Repo is ready for Plan 01-02 runtime/build validation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-modernization-baseline/01-01-SUMMARY.md`
</output>
