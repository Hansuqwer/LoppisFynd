---
phase: 04-core-screens-goldens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/dashboard/dashboard_screen.dart
  - lib/features/hauls/haul_screen.dart
  - lib/features/history/history_screen.dart
  - lib/features/history/widgets/coffee_cup_empty_state.dart
  - lib/features/drafts/draft_editor_screen.dart
  - lib/l10n/app_sv.arb
  - lib/l10n/app_en.arb
autonomous: false
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04
must_haves:
  truths:
    - "Home tab matches the Visual Reference Pack layout and uses real Swedish copy"
    - "Current Haul shows the reference summary row (including 'Totalt värde:') and offline-backed list rows"
    - "History empty state is reachable for a new user and matches the reference (search + pebble filters + coffee-cup empty)"
    - "Draft editor matches the reference layout and remains usable offline (edit fields + save)"
  artifacts:
    - path: "lib/features/dashboard/dashboard_screen.dart"
      provides: "Home (Dashboard) reference-parity layout and CTA wiring"
    - path: "lib/features/hauls/haul_screen.dart"
      provides: "Current Haul reference-parity summary + list UI over Drift streams"
    - path: "lib/features/history/history_screen.dart"
      provides: "History empty state UI (deterministic for new users)"
    - path: "lib/features/drafts/draft_editor_screen.dart"
      provides: "Draft editor stacked-glass + fields/actions per reference"
  key_links:
    - from: "lib/features/dashboard/dashboard_screen.dart"
      to: "lib/core/app/providers.dart"
      via: "deepLinkTabIndexProvider for Scan CTA"
      pattern: "deepLinkTabIndexProvider"
    - from: "lib/features/hauls/haul_screen.dart"
      to: "lib/core/app/providers.dart"
      via: "Drift DB access"
      pattern: "appDatabaseProvider"
    - from: "lib/features/drafts/draft_editor_screen.dart"
      to: "lib/core/app/providers.dart"
      via: "Drift DB access"
      pattern: "appDatabaseProvider"
---

<objective>
Reskin the core working screens (Home, Current Haul, History empty state, Draft editor) to Visual Reference Pack parity while preserving offline-first behavior and localization constraints.

Purpose: Deliver SCR-01..SCR-04 by retrofitting the existing offline-backed screens in-place, using established Nature Distilled tokens/primitives.
Output: Updated core screen widgets + localized copy additions and regenerated AppLocalizations.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-core-screens-goldens/04-RESEARCH.md

# Visual source of truth
@docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf

# Existing shell/primitives (do not rewrite)
@lib/core/navigation/app_nav_shell.dart
@lib/shared/widgets/nature_background.dart
@lib/shared/widgets/glass_surface.dart
@lib/shared/widgets/glass_board.dart
@lib/shared/widgets/stacked_backplates.dart
@lib/shared/widgets/bento_card.dart

# Target screens
@lib/features/dashboard/dashboard_screen.dart
@lib/features/hauls/haul_screen.dart
@lib/features/history/history_screen.dart
@lib/features/drafts/draft_editor_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring Home + Current Haul to reference parity (layout + copy + CTA wiring)</name>
  <files>
lib/features/dashboard/dashboard_screen.dart
lib/features/hauls/haul_screen.dart
lib/l10n/app_sv.arb
lib/l10n/app_en.arb
  </files>
  <action>
- Update `DashboardScreen` (Home tab) to match the Visual Reference Pack:
  - Compose with existing Nature Distilled primitives (prefer `GlassBoard`/`GlassSurface`, `BentoCard`, token typography/colors; do not add theme-wide overrides).
  - Implement the reference hero CTA and bento tile layout (spacing/radius/shadows per tokens; avoid ad-hoc numbers in shared primitives).
  - Ensure primary "scan" CTA deep-links to the Scan tab via `deepLinkTabIndexProvider` (do not change routes/tab contract).
  - Replace any placeholder copy with real Swedish copy via `AppLocalizations` only.

- Update `HaulScreen` (Current Haul tab) to match the Visual Reference Pack:
  - Render the summary row including the localized label "Totalt värde:" (use exact Swedish diacritics in ARB; no hardcoded strings).
  - Reskin list rows + primary camera/add action styling to match reference (shape/blur tokens/primitives), but keep the same underlying Drift watchers and write paths.
  - Ensure the add/scan affordance stays usable offline (do not gate on `isOnlineProvider` or `AppConfig.hasSupabase`).

- Localization wiring:
  - Add/update any required keys in `lib/l10n/app_sv.arb` + `lib/l10n/app_en.arb`.
  - Regenerate `lib/gen/app_localizations*.dart` using the repo's existing localization workflow.

Constraints (locked from prior phases):
- No ThemeData-wide input/button overrides (golden drift risk).
- Handwritten font accent-only.
- No new `BackdropFilter` outside shared glass primitives.
  </action>
  <verify>
- `flutter gen-l10n`
- `flutter analyze`
- `flutter test`
  </verify>
  <done>
- Home and Current Haul are visually structured to match the reference pack, using tokens/primitives.
- All new user-facing strings are in AppLocalizations (sv/en) and generated localizations compile.
- Core UI remains offline-first (no new online-only gating for local actions).
  </done>
</task>

<task type="auto">
  <name>Task 2: Bring History empty state + Draft editor to reference parity (and keep offline actions intact)</name>
  <files>
lib/features/history/history_screen.dart
lib/features/history/widgets/coffee_cup_empty_state.dart
lib/features/drafts/draft_editor_screen.dart
lib/l10n/app_sv.arb
lib/l10n/app_en.arb
  </files>
  <action>
- Update `HistoryScreen` to match the Visual Reference Pack **empty state** (this plan does not require reskinning the full non-empty history list/map):
  - Implement the reference search field styling and the "pebble" filter chips styling.
  - Ensure the coffee-cup empty state matches the reference composition and Swedish copy (use `CoffeeCupEmptyState`).
  - Make the empty state reliably reachable for a brand-new user:
    - Treat the current/default haul (from `defaultHaulIdProvider`) as "Current Haul" and exclude it from History list/empty checks.
    - Keep existing filtering/search state logic, but ensure it does not prevent empty rendering.

- Update `DraftEditorScreen` to match the Visual Reference Pack:
  - Use `StackedBackplates`/`GlassBoard` patterns for stacked glass sections.
  - Reskin the preview area, AI tag pills, and field sections; include the localized field label "Pris (SEK)".
  - Keep the same Drift-backed writes (e.g., DAO upserts) and ensure save/delete actions remain available offline.

- Localization wiring:
  - Add/update sv/en ARB keys for any new labels/hints/buttons.
  - Regenerate AppLocalizations outputs.
  </action>
  <verify>
- `flutter gen-l10n`
- `flutter analyze`
- `flutter test`
  </verify>
  <done>
- History empty state renders in a deterministic "new user" scenario and matches reference layout/copy.
- Draft editor matches reference layout/copy and remains offline-safe for editing/saving.
- No new hardcoded strings are introduced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify core screen parity + offline sanity</name>
  <files>
lib/features/dashboard/dashboard_screen.dart
lib/features/hauls/haul_screen.dart
lib/features/history/history_screen.dart
lib/features/drafts/draft_editor_screen.dart
docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf
  </files>
  <action>
- Run the app: `flutter run`.

- Compare each target screen to `docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf`:
  - Home (Dashboard): hero CTA + bento tiles layout and typography.
  - Current Haul: summary row ("Totalt värde:") + list row styling + camera/add action.
  - History: empty state (search + pebble filters + coffee cup).
  - Draft editor: stacked glass sections, AI tags, and fields including "Pris (SEK)".

- Quick offline sanity:
  - Enable airplane mode.
  - Confirm you can still open Haul, open a Draft, edit fields, and save locally (no blockers requiring network).

Resume signal:
- Type "approved" or describe the mismatches to address.
  </action>
  <verify>
- Record the result (approved or issue report). If issues: include which screen + what mismatched (layout/copy/component).
  </verify>
  <done>
- Verification complete (approved or issue report detailed enough for follow-up work).
  </done>
</task>

</tasks>

<verification>
- Core screens use tokens/primitives (no ThemeData-wide overrides introduced).
- All new UI copy is localized (sv/en) and uses correct diacritics.
</verification>

<success_criteria>
- SCR-01..SCR-04 truths are achievable in the running app, and the user confirms visual parity against the reference pack.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-screens-goldens/04-01-SUMMARY.md`
</output>
