---
phase: 04-core-screens-goldens
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/features/settings/settings_screen.dart
  - lib/l10n/app_sv.arb
  - lib/l10n/app_en.arb
  - lib/gen/app_localizations.dart
  - lib/gen/app_localizations_sv.dart
  - lib/gen/app_localizations_en.dart
  - test/features_settings/fl_068_settings_dev_mode_reveal_test.dart
autonomous: false
requirements:
  - SCR-05
must_haves:
  truths:
    - "Profile/Settings is clean by default and matches the Visual Reference Pack module layout"
    - "Settings exposes the three bento modules: 'Molnsynk & Data', 'AI & Modell', and 'Integritet'"
    - "Dev-only sync controls remain hidden for normal users and are only revealed via existing Dev Mode"
  artifacts:
    - path: "lib/features/settings/settings_screen.dart"
      provides: "Profile/Settings bento module UI and clean default presentation"
    - path: "test/features_settings/fl_068_settings_dev_mode_reveal_test.dart"
      provides: "Regression test for Dev Mode gated controls"
  key_links:
    - from: "lib/features/settings/settings_screen.dart"
      to: "dev_mode_enabled_v1"
      via: "existing Dev Mode gating"
      pattern: "dev_mode_enabled_v1"
---

<objective>
Reskin the Profile/Settings tab to Visual Reference Pack parity, presenting the referenced bento modules while keeping the screen clean by default and preserving the existing Dev Mode gating for advanced controls.

Purpose: Satisfy SCR-05 without regressing offline-first behavior or exposing sync controls to normal users.
Output: Updated SettingsScreen layout + localized module titles/copy + updated tests for Dev Mode gating.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-core-screens-goldens/04-RESEARCH.md

# Visual source of truth
@docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf

# Target screen + existing Dev Mode regression
@lib/features/settings/settings_screen.dart
@test/features_settings/fl_068_settings_dev_mode_reveal_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SettingsScreen into the reference bento modules (clean default)</name>
  <files>
lib/features/settings/settings_screen.dart
  </files>
  <action>
- Update `SettingsScreen` (Profile tab) to match the Visual Reference Pack:
  - Present a clean, uncluttered default view using existing Nature Distilled primitives (glass boards + bento cards).
  - Implement the three referenced module groups (as bento modules/sections):
    - "Molnsynk & Data"
    - "AI & Modell"
    - "Integritet"
  - Keep navigation/IA stable: do not rename routes/tabs or invent new primary destinations.

- Dev Mode constraint (user preference + prior implementation):
  - Ensure any sync controls / debug toggles remain hidden unless Dev Mode is enabled via the existing 7-tap gesture.
  - Avoid surfacing sync intervals / workmanager controls to normal users.

- Use `AppLocalizations` for all new section titles and row labels (no hardcoded strings).
  </action>
  <verify>
- `flutter analyze`
- `flutter test test/features_settings/fl_068_settings_dev_mode_reveal_test.dart`
  </verify>
  <done>
- Settings screen visually matches the reference module structure and is clean by default.
- Dev-only controls are not visible without Dev Mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Localize new Settings module copy and refresh Dev Mode gating test expectations</name>
  <files>
lib/l10n/app_sv.arb
lib/l10n/app_en.arb
lib/gen/app_localizations.dart
lib/gen/app_localizations_sv.dart
lib/gen/app_localizations_en.dart
test/features_settings/fl_068_settings_dev_mode_reveal_test.dart
  </files>
  <action>
- Add/update sv/en localization keys for the Settings module headers and any new row labels introduced in Task 1.
- Regenerate `lib/gen/app_localizations*.dart` using the repo's established localization workflow.
- Update `test/features_settings/fl_068_settings_dev_mode_reveal_test.dart` expectations if the new UI structure changes finder targets, but keep the test's intent:
  - Dev Mode reveals advanced controls.
  - Normal mode stays clean and does not show sync controls.
  </action>
  <verify>
- `flutter test test/features_settings/fl_068_settings_dev_mode_reveal_test.dart`
- `flutter test`
  </verify>
  <done>
- All new Settings UI copy is localized (sv/en) and generated localizations compile.
- Dev Mode reveal regression test passes and still asserts the intended gating behavior.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Settings modules parity + Dev Mode gating</name>
  <files>
lib/features/settings/settings_screen.dart
docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf
  </files>
  <action>
- Run the app: `flutter run`.

- Open Profile tab and compare with `docs/LoppisFynd_Nature_Distilled_Visual_Reference_Pack.pdf`:
  - Module groups exist with correct Swedish titles ("Molnsynk & Data", "AI & Modell", "Integritet").
  - Default view is clean (no dev/sync controls visible).

- Trigger Dev Mode (existing 7-tap gesture) and confirm the dev-only controls appear only after Dev Mode is enabled.

Resume signal:
- Type "approved" or describe what looks off.
  </action>
  <verify>
- Record the result (approved or issue report). If issues: include which module/row and what mismatch.
  </verify>
  <done>
- Verification complete (approved or issue report detailed enough for follow-up work).
  </done>
</task>

</tasks>

<verification>
- Dev Mode gating remains intact and tested.
- No hardcoded user-facing strings introduced.
</verification>

<success_criteria>
- SCR-05 is achieved and visually verified against the reference pack, with Dev Mode-only controls hidden by default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-screens-goldens/04-02-SUMMARY.md`
</output>
