---
phase: 04-core-screens-goldens
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - test/features_auth/login_screen_golden_test.dart
  - test/features_dashboard/dashboard_screen_golden_test.dart
  - test/features_history/history_empty_golden_test.dart
  - test/features_drafts/draft_editor_golden_test.dart
  - test/fl_070_offline_core_screens_smoke_test.dart
  - test/goldens/login_screen.png
  - test/goldens/dashboard_screen.png
  - test/goldens/history_empty.png
  - test/goldens/draft_editor.png
autonomous: true
requirements:
  - QA-01
  - OFF-01
must_haves:
  truths:
    - "Golden tests exist for Login, Home, History empty, and Draft editor and pass in CI"
    - "Core screens render and remain usable when the app is offline (no new online-only blockers introduced)"
  artifacts:
    - path: "test/features_dashboard/dashboard_screen_golden_test.dart"
      provides: "Home (Dashboard) golden regression coverage"
    - path: "test/features_history/history_empty_golden_test.dart"
      provides: "History empty-state golden regression coverage"
    - path: "test/features_drafts/draft_editor_golden_test.dart"
      provides: "Draft editor golden regression coverage"
    - path: "test/fl_070_offline_core_screens_smoke_test.dart"
      provides: "Offline-first smoke regression coverage"
  key_links:
    - from: "test/features_dashboard/dashboard_screen_golden_test.dart"
      to: "lib/features/dashboard/dashboard_screen.dart"
      via: "widget pump with ProviderScope overrides"
      pattern: "ProviderScope"
---

<objective>
Lock the Phase 4 visuals in place with golden tests for the minimum key screens and add an offline-first regression smoke test so future reskins don’t introduce online-only blockers.

Purpose: Satisfy QA-01 and OFF-01 by turning the reference-parity screens into regression-protected snapshots and ensuring offline behavior remains intact.
Output: New/updated golden tests + checked-in golden PNGs + an offline smoke test.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-core-screens-goldens/04-RESEARCH.md

# Existing golden harness examples
@test/features_auth/login_screen_golden_test.dart
@test/fl_066_bento_card_golden_test.dart
@test/fl_065_nav_smoke_test.dart

# App shell + providers used in tests
@lib/core/navigation/app_nav_shell.dart
@lib/core/app/providers.dart
@lib/core/database/app_database.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add goldens for Home, History empty, and Draft editor (and keep Login golden up to date)</name>
  <files>
test/features_auth/login_screen_golden_test.dart
test/features_dashboard/dashboard_screen_golden_test.dart
test/features_history/history_empty_golden_test.dart
test/features_drafts/draft_editor_golden_test.dart
test/goldens/login_screen.png
test/goldens/dashboard_screen.png
test/goldens/history_empty.png
test/goldens/draft_editor.png
  </files>
  <action>
- Follow the repo's existing golden conventions (see `test/features_auth/login_screen_golden_test.dart` and `test/fl_066_bento_card_golden_test.dart`):
  - Disable runtime font fetching: `GoogleFonts.config.allowRuntimeFetching = false;`
  - Set a deterministic surface size (e.g., iPhone 12-ish 390x844) and use `pumpAndSettle` until idle.
  - Use `MaterialApp(theme: AppTheme.light(), locale: const Locale('sv'), ...)`.
  - For app-backed screens, pump inside `ProviderScope` with an in-memory Drift DB (`AppDatabase.inMemory()`), seeded with minimal deterministic rows required for the target visual state.

- Create new golden tests and snapshots:
  - `test/features_dashboard/dashboard_screen_golden_test.dart` -> `test/goldens/dashboard_screen.png` (Home tab).
  - `test/features_history/history_empty_golden_test.dart` -> `test/goldens/history_empty.png` (History empty state; ensure the DB seed triggers the empty condition per Phase 4 logic).
  - `test/features_drafts/draft_editor_golden_test.dart` -> `test/goldens/draft_editor.png` (Draft editor; seed a draft row so editor renders deterministically).

- Keep login golden stable:
  - Update `test/features_auth/login_screen_golden_test.dart` and `test/goldens/login_screen.png` only if Phase 4 changes incidentally affected shared primitives/fonts.

- Generate/update the PNGs using `flutter test --update-goldens` (only for intentional baseline changes), and ensure `flutter test` passes afterwards.
  </action>
  <verify>
- `flutter test test/features_auth/login_screen_golden_test.dart`
- `flutter test test/features_dashboard/dashboard_screen_golden_test.dart`
- `flutter test test/features_history/history_empty_golden_test.dart`
- `flutter test test/features_drafts/draft_editor_golden_test.dart`
- `flutter test`
  </verify>
  <done>
- Goldens exist for Login, Home, History empty, and Draft editor, and they pass locally.
- Golden snapshots are deterministic (no flaky animations/async).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add an offline-first regression smoke test for core flows/screens</name>
  <files>
test/fl_070_offline_core_screens_smoke_test.dart
  </files>
  <action>
- Add `test/fl_070_offline_core_screens_smoke_test.dart` that asserts OFF-01 at a practical level:
  - Pump `AppNavShell` (or the target screens directly) inside `ProviderScope` with `AppDatabase.inMemory()`.
  - Override providers so the app is "offline" (`isOnlineProvider` false) and optional integrations are disabled when appropriate (e.g., no Supabase) to simulate the offline baseline.
  - Seed minimal DB rows to ensure the screens render meaningful UI.
  - Assert that key local actions are still present/enabled (e.g., open Draft editor, edit a field, tap save; navigate to Haul/History without blockers).

- Keep it deterministic and fast: no network, no timers beyond a short `pumpAndSettle`.
  </action>
  <verify>
- `flutter test test/fl_070_offline_core_screens_smoke_test.dart`
- `flutter test`
  </verify>
  <done>
- There is automated regression coverage that fails if Phase 4 UI introduces new online-only blockers for core screens.
  </done>
</task>

</tasks>

<verification>
- `flutter test` passes without updating goldens.
- Golden diffs are intentional and match the reference parity work.
</verification>

<success_criteria>
- QA-01 and OFF-01 are satisfied via automated tests that pass in CI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-screens-goldens/04-03-SUMMARY.md`
</output>
