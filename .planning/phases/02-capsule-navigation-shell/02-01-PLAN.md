---
phase: 02-capsule-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/app_nav_shell.dart
  - lib/shared/widgets/capsule_nav_bar.dart
  - lib/shared/widgets/nature_background.dart
  - lib/features/scanner/scanner_screen.dart
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Switching between tabs preserves in-tab state (no rebuild resets)"
    - "Nature background persists behind all tabs and content is not obscured by the capsule nav"
  artifacts:
    - "lib/core/navigation/app_nav_shell.dart: keep-alive tab host (lazy cached IndexedStack)"
    - "lib/shared/widgets/capsule_nav_bar.dart: capsule obstruction-height helper for safe insets"
    - "lib/features/scanner/scanner_screen.dart: scanner active/inactive lifecycle for keep-alive shells"
  key_links:
    - "lib/core/navigation/app_nav_shell.dart -> lib/shared/widgets/capsule_nav_bar.dart: bottom padding derived from capsule obstruction height"
    - "lib/core/navigation/app_nav_shell.dart -> lib/features/scanner/scanner_screen.dart: passes tab active state"
---

<objective>
Replace the app shell tab host with a lazy keep-alive IndexedStack, while keeping the Nature background and ensuring content padding matches the real capsule obstruction height.

Purpose: Make tab switching preserve scroll/input state without triggering expensive tab initialization at startup.
Output: A stable 5-tab shell that keeps visited tabs alive, avoids eager scanner camera init, and prevents capsule overlap via computed insets.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capsule-navigation-shell/02-RESEARCH.md

@lib/core/navigation/app_nav_shell.dart
@lib/shared/widgets/capsule_nav_bar.dart
@lib/shared/widgets/nature_background.dart
@lib/features/scanner/scanner_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lazy cached IndexedStack tab host</name>
  <files>lib/core/navigation/app_nav_shell.dart</files>
  <action>
Refactor the shell body so it keeps visited tabs alive without eagerly building all 5 tabs.

- Replace the current `switch (_tab)` tab swap with a lazy-cached `IndexedStack`.
- Ensure only the currently selected tab is built on first render; other tabs must start as placeholders and be built only when first selected.
- Wrap each tab subtree in `TickerMode(enabled: selected)` so animations do not run while offstage.
- Keep the existing 5-tab order and destination keys (`nav_dashboard`, `nav_scanner`, `nav_haul`, `nav_history`, `nav_profile`) unchanged.
- Preserve existing deep-link behavior via `deepLinkTabIndexProvider` and `deepLinkScanItemIdProvider` (no route/name regression).

Implementation notes (to avoid scanner regressions):
- Cache should store built widgets per index (e.g. `Map<int, Widget>`).
- Always ensure the selected index is built before returning the `IndexedStack`.
- Add a debug `assert(AppTab.values.length == 5)` to make the contract explicit.
  </action>
  <verify>flutter test test/fl_065_nav_smoke_test.dart</verify>
  <done>
- `AppNavShell` uses a lazy keep-alive `IndexedStack` (no `switch (_tab)` body swap).
- Initial app start does not build the Scan tab subtree until first selected.
- `test/fl_065_nav_smoke_test.dart` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Centralize capsule obstruction height and prevent offstage scanner camera work</name>
  <files>
lib/shared/widgets/capsule_nav_bar.dart
lib/core/navigation/app_nav_shell.dart
lib/features/scanner/scanner_screen.dart
  </files>
  <action>
Remove hardcoded shell bottom padding and ensure the scanner doesn't keep camera resources running offstage.

Capsule obstruction height:
- In `lib/shared/widgets/capsule_nav_bar.dart`, extract the existing bottom margin computation into a reusable helper (no behavior change).
- Add a public helper (e.g. `CapsuleNavBar.obstructionHeight(context)`) returning `AppCapsuleNav.barHeight + marginBottom`.
- In `lib/core/navigation/app_nav_shell.dart`, replace `EdgeInsets.only(bottom: 108)` with bottom padding derived from the capsule obstruction helper.

Nature background contract:
- In `lib/core/navigation/app_nav_shell.dart`, use the contract-named `NatureBackground` widget (not `AtmosphericBackground`) for the shell background.

Scanner active/inactive lifecycle:
- Add an `active` boolean to `ScannerScreen` as an *optional named parameter* with a default of `true` (backwards compatible for any non-shell call sites).
- In `lib/core/navigation/app_nav_shell.dart`, pass `active: _tab == AppTab.scanner` explicitly when building the Scan tab.
- Gate camera init/warmup so they run only when `active == true`.
- When `active` transitions true->false, dispose the camera controller and stop any image stream work so the camera is not kept alive while offstage.
- When `active` transitions false->true, re-run camera init.
  </action>
  <verify>flutter test test/fl_065_nav_smoke_test.dart && flutter analyze && flutter pub run custom_lint</verify>
  <done>
- Shell bottom padding tracks capsule height + safe-area margin (no `108` magic number).
- Nature background persists behind all tabs via `NatureBackground`.
- After visiting Scan, switching away stops camera usage; returning to Scan re-initializes camera.
- Tests + analyze + custom_lint all succeed.
  </done>
</task>

</tasks>

<verification>
- `flutter test test/fl_065_nav_smoke_test.dart`
- `flutter analyze`
- `flutter pub run custom_lint`
</verification>

<success_criteria>
- Switching tabs preserves stateful UI (scroll positions/inputs) for previously visited tabs.
- Content is never obscured by the capsule nav because shell padding uses capsule obstruction height.
- Scan tab does not initialize camera until first selected and releases camera when tab becomes inactive.
</success_criteria>

<output>
After completion, create `.planning/phases/02-capsule-navigation-shell/02-01-SUMMARY.md`
</output>
