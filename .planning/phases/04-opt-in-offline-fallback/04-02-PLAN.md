---
phase: 04-opt-in-offline-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/core/app/providers.dart
  - lib/core/settings/app_settings_keys.dart
  - lib/services/offline_detection/offline_detection_types.dart
  - lib/services/offline_detection/offline_detector.dart
  - lib/services/offline_detection/offline_detector_parser.dart
  - test/services_offline_detection/offline_detector_parser_test.dart
autonomous: true
requirements:
  - OFF-01
  - OFF-03

must_haves:
  truths:
    - "Offline detection returns evidence (boxes + confidence) and filters low-confidence results"
    - "Inference can run without network connectivity"
    - "Detection work runs off the UI isolate (no jank by design)"
  artifacts:
    - path: "lib/services/offline_detection/offline_detection_types.dart"
      provides: "Evidence-first result schema (normalized boxes + score + label)"
      contains: "class OfflineDetection"
    - path: "lib/services/offline_detection/offline_detector.dart"
      provides: "Offline detector service using local TFLite model"
      contains: "class OfflineDetector"
    - path: "lib/services/offline_detection/offline_detector_parser.dart"
      provides: "Model-output parsing into OfflineDetection evidence"
      contains: "parse"
  key_links:
    - from: "lib/services/offline_detection/offline_detector.dart"
      to: "lib/services/offline_detection/offline_detection_types.dart"
      via: "returns OfflineDetections"
      pattern: "OfflineDetection"
    - from: "lib/core/app/providers.dart"
      to: "lib/services/offline_detection/offline_detector.dart"
      via: "Riverpod provider wiring"
      pattern: "offlineDetectorProvider"
---

<objective>
Implement the offline detection runtime that produces evidence (bounding boxes + confidence) suitable for the Phase 4 split-view UI.

Purpose: Provide an on-device, network-free fallback producing stable evidence objects and enforcing a hard confidence threshold.
Output: Offline detector service + evidence schema + provider wiring + parser tests.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-opt-in-offline-fallback/04-CONTEXT.md
@.planning/phases/04-opt-in-offline-fallback/04-RESEARCH.md

@lib/core/app/providers.dart
@lib/services/ai/model_manager.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add offline evidence types + confidence banding with hard cutoff</name>
  <files>lib/services/offline_detection/offline_detection_types.dart</files>
  <action>
Create `lib/services/offline_detection/offline_detection_types.dart` for an evidence-first schema that the UI can render.

- Implement `NormalizedRect(left, top, right, bottom)` normalized to 0..1.
- Implement `OfflineDetection(label, score, box)` where `score` is 0..1.
- Add confidence helpers per locked decision:
  - A hard minimum threshold constant (e.g. `kOfflineDetectionMinScore`). Detections below this are not surfaced.
  - A function mapping score to both percent display and High/Med/Low label.
- Ensure the mapping is deterministic and centralized (UI must not re-implement banding).
  </action>
  <verify>flutter analyze</verify>
  <done>
Evidence types exist, hard cutoff is defined, and a single shared function maps score -> High/Med/Low label.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement offline detector parser + tests (evidence correctness)</name>
  <files>
lib/services/offline_detection/offline_detector_parser.dart
test/services_offline_detection/offline_detector_parser_test.dart
  </files>
  <action>
Create a pure-Dart parsing layer that turns typical SSD/EfficientDet outputs into `OfflineDetection` evidence.

- Implement `parseDetections(...)` that accepts:
  - raw `boxes` (N x 4), `scores` (N), `classes` (N), and `count` (num detections)
  - a label resolver (class index -> label)
- Output must:
  - clamp/validate normalized box coords
  - filter detections below `kOfflineDetectionMinScore`
  - sort descending by score

Write unit tests validating:
- below-threshold filtering
- sorting
- box normalization/clamping behavior
- stable mapping to `OfflineDetection`
  </action>
  <verify>flutter test test/services_offline_detection/offline_detector_parser_test.dart</verify>
  <done>
Parser unit tests pass and cover threshold filtering + ordering + box normalization.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement OfflineDetector service + Riverpod wiring</name>
  <files>
lib/core/settings/app_settings_keys.dart
lib/core/app/providers.dart
lib/services/offline_detection/offline_detector.dart
  </files>
  <action>
Implement the service entry point that runs TFLite inference off the UI isolate and returns parsed evidence.

- Add settings keys to `lib/core/settings/app_settings_keys.dart`:
  - `kOfflineIdentificationEnabledKeyV1` (0/1)
  - `kOfflineModelDownloadSuggestionShownKeyV1` (0/1) (used later in Settings)
- In `lib/core/app/providers.dart`, add:
  - `offlineIdentificationEnabledProvider` (StreamProvider<bool>) reading `kOfflineIdentificationEnabledKeyV1` (default OFF when unset).
  - `offlineDetectorProvider` constructing an `OfflineDetector`.
- Create `lib/services/offline_detection/offline_detector.dart`:
  - Accept dependencies required to locate the installed model file and run inference (e.g. `OfflineModelSpec` + `ModelManager` + label map).
  - Use `tflite_flutter` and run inference off the UI isolate (prefer `IsolateInterpreter`); do not block widget builds.
  - Use `package:image` to decode and bake EXIF orientation before preprocessing.
  - Parse outputs via `offline_detector_parser.dart`.
- Do NOT add any UI here; this is a service-only plan.
  </action>
  <verify>flutter test</verify>
  <done>
`OfflineDetector` can be constructed via providers, reads local model file, and produces evidence objects through the shared parser without requiring network connectivity.
  </done>
</task>

</tasks>

<verification>
- `dart format --output=none --set-exit-if-changed lib test`
- `flutter analyze`
- `flutter test`
</verification>

<success_criteria>
- Offline evidence schema and confidence threshold exist in a shared types file.
- A tested parser turns model outputs into stable evidence suitable for UI overlays.
- OfflineDetector service is wired through Riverpod and can run without network.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opt-in-offline-fallback/04-02-SUMMARY.md`
</output>
