---
phase: 03-sold-price-comps-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/features/settings/settings_screen.dart
autonomous: false
requirements:
  - MKT-01

gap_closure: true

must_haves:
  truths:
    - "Toggling 'Fetch sold-price comps' immediately (re)schedules/cancels background comps refresh without requiring an app restart"
    - "When background comps are enabled and proxy is configured, periodic background refresh can run best-effort"
  artifacts:
    - path: "lib/features/settings/settings_screen.dart"
      provides: "Settings comps toggle triggers BackgroundSync.scheduleIfConfigured so background work state updates immediately"
  key_links:
    - from: "lib/features/settings/settings_screen.dart"
      to: "lib/services/sync/background/background_sync.dart"
      via: "Call to BackgroundSync.scheduleIfConfigured after persisting comps toggle"
      pattern: "BackgroundSync\\.scheduleIfConfigured"
---

<objective>
Close Phase 3 verification gap: changing the sold-price comps toggle in Settings must immediately update Workmanager periodic scheduling (cancel when OFF, schedule when ON) so background comps refresh starts/stops without requiring an app restart.

Purpose: Make background comps behavior reliable and user-controllable by ensuring Settings changes trigger scheduling changes at the moment the user flips the switch.
Output: Settings comps toggle calls `BackgroundSync.scheduleIfConfigured` after persisting the new value, plus a manual verification checkpoint.
</objective>

<execution_context>
@/home/hans/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hans/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sold-price-comps-hardening/03-VERIFICATION.md

@lib/features/settings/settings_screen.dart
@lib/services/sync/background/background_sync.dart
@lib/core/settings/app_settings_keys.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reschedule background comps work when the Settings toggle changes</name>
  <files>
lib/features/settings/settings_screen.dart
  </files>
  <action>
Fix the Phase 3 verification gap by ensuring the "Fetch sold-price comps" toggle immediately updates background scheduling.

Implementation requirements:
- In `_buildPrivacyModule(...)`, in the `SwitchListTile.adaptive` for `kPrivacyFetchSoldPriceCompsEnabledKeyV1`:
  - After persisting the new value via `db.appSettingsDao.setInt(...)`, call `BackgroundSync.scheduleIfConfigured(db: db)`.
  - Do this for BOTH transitions (enable and disable); do not rely on app restart or other entrypoints.
- Keep behavior consistent with existing interval dropdown (which already calls `BackgroundSync.scheduleIfConfigured(db: db)` after changes).
- Do not add new strings/localization in this gap closure; keep it a functional wiring fix.

Notes:
- `BackgroundSync.scheduleIfConfigured` already handles: unsupported platforms, comps disabled, missing `TRADERA_PROXY_URL`, and interval <= 0 by canceling work.
- This task should not change the persisted defaults or gating semantics (null => enabled, non-1 => disabled).
  </action>
  <verify>
1) `flutter analyze`
2) `flutter test`
  </verify>
  <done>
- Flipping the comps toggle in Settings calls `BackgroundSync.scheduleIfConfigured` after writing the setting.
- No new analyzer errors introduced and tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify background scheduling reacts immediately to comps toggle</name>
  <files>
lib/features/settings/settings_screen.dart
lib/services/sync/background/background_sync.dart
  </files>
  <action>
Claude wired the Settings comps toggle to re-run `BackgroundSync.scheduleIfConfigured` immediately after changes.

How to verify (Android emulator/device):
1) Run the app with a Tradera proxy configured:
   - `flutter run --dart-define=TRADERA_PROXY_URL=https://<project>.supabase.co/functions/v1/tradera-proxy`
2) In Settings, toggle "Fetch sold-price comps" OFF.
   - Expected: background periodic work is canceled (best-effort). You should not see background comps attempts continue.
3) Toggle it ON again (without restarting the app).
   - Expected: periodic work gets (re)scheduled; background comps refresh can start running best-effort.

If you have log access for Workmanager scheduling/execution, use it to confirm a schedule/cancel event occurs immediately after each toggle.
  </action>
  <verify>Complete the steps and reply "approved" (or describe the issue observed).</verify>
  <done>User replied "approved" (or provided concrete issues to address).</done>
</task>

</tasks>

<verification>
- Re-run Phase 3 verification truth #2 (from `.planning/phases/03-sold-price-comps-hardening/03-VERIFICATION.md`) after applying this fix.
</verification>

<success_criteria>
- MKT-01: Background comps refresh attempts happen automatically when enabled, and enabling/disabling via Settings immediately updates background scheduling.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sold-price-comps-hardening/03-03-SUMMARY.md`
</output>
